# إعدادات الذاكرة لتسريع البناء
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# تفعيل دعم مكتبات AndroidX (هذا السطر هو الذي يحل الخطأ)
android.useAndroidX=true

# تفعيل التوافق مع المكتبات القديمة (احتياطياً)
android.enableJetifier=true
