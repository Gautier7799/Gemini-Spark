name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: List Repository Contents and Locate Project
        id: locate_project
        run: |
          SETTINGS_FILE=$(find . -name "settings.gradle.kts" -o -name "settings.gradle" | head -n 1)
          if [ -z "$SETTINGS_FILE" ]; then
            echo "::error::لم يتم العثور على ملف settings.gradle.kts!"
            exit 1
          fi
          PROJECT_DIR=$(dirname "$SETTINGS_FILE")
          echo "project_dir=$PROJECT_DIR" >> $GITHUB_OUTPUT

      # الدرع الواقي: إنشاء أي ملفات ناقصة لضمان نجاح الـ Build
      - name: Ensure Required Resources Exist
        working-directory: ${{ steps.locate_project.outputs.project_dir }}
        run: |
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/mipmap-anydpi-v26
          mkdir -p app/src/main/res/drawable
          
          # تأمين ملف strings.xml
          if [ ! -f "app/src/main/res/values/strings.xml" ]; then
            cat <<EOF > app/src/main/res/values/strings.xml
          <resources>
              <string name="app_name">Gemini Spark</string>
          </resources>
          EOF
          fi
          
          # تأمين ملف themes.xml
          if [ ! -f "app/src/main/res/values/themes.xml" ]; then
            cat <<EOF > app/src/main/res/values/themes.xml
          <resources>
              <style name="Theme.MyApplication" parent="android:Theme.Material.Light.NoActionBar" />
          </resources>
          EOF
          fi

          # تأمين ملف خلفية الأيقونة (Background)
          if [ ! -f "app/src/main/res/drawable/ic_launcher_background.xml" ]; then
            cat <<EOF > app/src/main/res/drawable/ic_launcher_background.xml
          <vector xmlns:android="http://schemas.android.com/apk/res/android" android:width="108dp" android:height="108dp" android:viewportWidth="108" android:viewportHeight="108">
              <path android:fillColor="#00001a" android:pathData="M0,0h108v108h-108z" />
          </vector>
          EOF
          fi

          # تأمين ملف الطبقة الأمامية للأيقونة (Foreground)
          if [ ! -f "app/src/main/res/drawable/ic_launcher_foreground.xml" ]; then
            cat <<EOF > app/src/main/res/drawable/ic_launcher_foreground.xml
          <vector xmlns:android="http://schemas.android.com/apk/res/android" android:width="108dp" android:height="108dp" android:viewportWidth="108" android:viewportHeight="108">
              <path android:fillColor="#FFFFFF" android:pathData="M54,20 l20,40 l-40,0 z" />
          </vector>
          EOF
          fi

          # تأمين ملفات الـ mipmap التي تربط الأيقونات ببعضها
          if [ ! -f "app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml" ]; then
            cat <<EOF > app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@drawable/ic_launcher_background" />
              <foreground android:drawable="@drawable/ic_launcher_foreground" />
          </adaptive-icon>
          EOF
          fi

          if [ ! -f "app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml" ]; then
            cat <<EOF > app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@drawable/ic_launcher_background" />
              <foreground android:drawable="@drawable/ic_launcher_foreground" />
          </adaptive-icon>
          EOF
          fi

      - name: Setup Environment Files
        working-directory: ${{ steps.locate_project.outputs.project_dir }}
        run: |
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY || 'MY_GEMINI_API_KEY' }}" > .env
          echo "GEMINI_API_KEY=MY_GEMINI_API_KEY" > .env.example

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Prepare Gradle Wrapper
        working-directory: ${{ steps.locate_project.outputs.project_dir }}
        run: |
          gradle wrapper --gradle-version 8.10.2
          chmod +x gradlew

      - name: Build APK with Gradle
        working-directory: ${{ steps.locate_project.outputs.project_dir }}
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Gemini-Spark-Debug-APK
          path: ${{ steps.locate_project.outputs.project_dir }}/app/build/outputs/apk/debug/*.apk
          retention-days: 14
